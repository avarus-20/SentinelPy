# Post v1.1.0 audit — SentinelPy

**Date:** 2026-09-19  
**Baseline:** `main` at tag `v1.1.0`

## Summary

v1.1.0 delivered engineering-trust items without changing scan findings or top-level public API. One **P1** compatibility gap was reported post-release (Codex on PR #27): removal of `evaluate_presence_findings` import. Addressed in **v1.1.1**.

## Verified

- Top-level `sentinelpy` exports unchanged; `run_scan` / CLI behavior unchanged.
- JSON reports still validate against `docs/report-schema-1.0.0.json` (84 pytest cases, ~89% coverage).
- CSP bodies still excluded from public evidence; invalid URLs still return structured errors.
- Runtime dependencies remain stdlib-only; `jsonschema` is dev/CI-only.
- Release workflow produces wheel + sdist for `v1.1.0`.

## Open items (P2+)

| Priority | Item | Notes |
| --- | --- | --- |
| P2 | Legacy `sentinelpy.main` raw headers | Documented; migrate integrators to `run_scan`. |
| P2 | Markdown/terminal renderers partial coverage | Smoke tests exist; not full branch coverage. |
| P2 | Dependabot Action version bumps vs SHA pins | Manual SHA updates when upgrading actions. |

## v1.2 direction

Optional `--no-color` for terminal output on TTY; CLI test for `--format markdown`; expand schema tests for every error category fixture.

## Remediation log

No post-v1.1.0 code fixes required.
