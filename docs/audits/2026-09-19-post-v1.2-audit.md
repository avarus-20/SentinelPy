# Post v1.2.0 audit — SentinelPy

**Date:** 2026-09-19  
**Baseline:** `main` at tag `v1.2.0`

## Summary

v1.2.0 adds CLI `--no-color` and Markdown e2e tests without changing scan logic or runtime dependencies. No **P0** or **P1** defects confirmed.

## Verified

- `--no-color` disables ANSI on TTY terminal output; JSON/Markdown byte-identical with or without the flag.
- Markdown CLI path excludes secrets (e2e tests with sensitive header fixtures).
- 91 pytest cases, ~88% coverage; CI matrix unchanged.

## Open (P2+)

- Legacy `evaluate_presence_findings` remains deprecated until 2.0.
- Markdown renderer branch coverage for redirects section still partial.

## Remediation

None required.
